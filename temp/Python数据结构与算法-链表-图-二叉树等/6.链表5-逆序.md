# 链表逆序

什么是逆序

就是首变尾 尾变首



原链表：

![image-20200526175653511](assets/image-20200526175653511.png)

逆序之后的链表：

![image-20200526175722046](assets/image-20200526175722046.png)



代码实现：

```python
class LinkNode:
    def __init__(self, item=None):
        self.item = item
        self.next = None

class SingleLink:
    def __init__(self):
        self.__head = None
    
    def __str__(self):
        val_list = list()
        temp = self.__head
        while temp != None:
            val_list.append(str(temp.item))
            temp = temp.next
        return "->".join(val_list)

    def append(self, item):
        # 创建一个节点
        new_node = LinkNode(item)

        # 如果链表是：空链表
        if not self.__head:
            self.__head = new_node
            return
        
        # 找到最后一个节点
        temp = self.__head
        while temp.next != None:
            temp = temp.next

        # 将新节点追加到末尾节点的后面
        temp.next = new_node

    def reverse(self):
        # 如果是空链表 或者 只有1个节点，就直接返回
        if self.__head == None or self.__head.next == None:
            return

        # 普通链表进行逆序
        pre = None  # 前驱节点
        cur = None  # 当前节点
        next = None  # 后继节点
        # 把链表首节点变为尾节点
        cur = self.__head
        next = cur.next
        cur.next = None
        pre = cur
        cur = next
        # 使当前遍历节点cur指向前驱节点
        while cur.next != None:
            next = cur.next
            cur.next = pre
            pre = cur
            cur = next
        # 遍历到链表最后一个节点时候，将其指向前驱节点
        cur.next = pre
        # 头节点指向原来链表的最后一个节点
        self.__head = cur


if __name__ == "__main__":

    # 创建链表对象
    link = SingleLink()

    # 追加数据到链表末尾
    link.append(11)
    link.append(22)
    link.append(33)
    link.append(44)
    link.append(55)

    # 遍历
    print(link)

    # 逆序
    link.reverse()

    # 遍历
    print(link)

```

运行效果：

```
11->22->33->44->55
55->44->33->22->11
```

